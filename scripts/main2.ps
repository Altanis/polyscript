export const DEBUG: bool = false;
export fn is_div_by_2(x: int): bool {
    x % 2 == 0
}

export struct Scalar {
    private x: int;
}

impl Scalar {
    fn new(x: int): Scalar {
        Scalar { x }
    }

    fn get_x(&self): int {
        self.x
    }
}