const x: int = 3;
let y = &x;
// let z = &mut x; -> err

struct Scalar {
    public inner: int;
}

impl Scalar {
    // fn mutate_self(&this) { this.inner += 1; } -> err
    fn mutate_self(&mut this) {
        this.inner += 1;
    }
}