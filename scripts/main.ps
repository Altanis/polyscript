struct MyStruct {}

trait Printable {
    fn print(&this): int;
}

impl Printable for MyStruct {
    fn print(&this): int {
        return 3;
    }
}

impl MyStruct {
    fn print(&this): char {
        return '3';
    }
}

fn main() {
    let s: MyStruct = MyStruct {};
    let instance_printed: char = s.print();
    let trait_printed: int = [MyStruct as Printable].print(&s);
}