// check if trait signature of default trait conforms
// operator overloading
// generics need to be constrained to traits properly
// memory safety (reference counting)


struct Scalar { x: int; }
impl Add<Scalar> for Scalar {
    type Output = Scalar;
    fn add(&this): Self {
        Scalar {
            x: this.x
        }
    }
}