trait Animal {
    type SpeechEmission;
    fn speak(&this): Self.SpeechEmission;
}

struct Dog {}

impl Animal for Dog {
    type SpeechEmission = char;
    fn speak(&this): Self.SpeechEmission {
        return "1";
    }
}