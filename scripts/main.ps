trait Animal {
    type SpeechEmission;

    fn speak(&this): Self.SpeechEmission;
}

struct Dog {}

impl Animal for Dog {
    type SpeechEmission = char;
    fn speak(&this): Self.SpeechEmission {
        return 'b';
    }
}

let dog = Dog {};
let f: string = dog.speak();