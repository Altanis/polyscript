struct Vector3D[T, U] {
    x: T;
    y: U;
    z: float;
}

impl Vector3D[int, float] {
    fn default(): Self {
        return Vector3D { x: 0, y: 0.0, z: 0.0 };
    }
}

impl[T] Vector3D[T] {
    fn dot(&this, other: &Self): int {
        return 0;
    }
}

let v1: Vector3D[int, float] = Vector3D.default();
let v2: Vector3D[char, string] = Vector3D {
    x: '0', y: "0", z: 1.0
};

let resultant: int = v1.operation(v2);