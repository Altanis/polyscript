struct Point[T] {
    public x: T;
    public y: T;
}

impl Add for Point[int] {
    type Output = int;

    fn add(this, other: Self): Self.Output {
        return Point {
            x: self.x + other.x,
            y: self.y + other.y
        };
    }
}

impl Add for Point[float] {
    type Output = float;

    fn add(this, other: Self): Self.Output {
        return Point {
            x: self.x + other.x,
            y: self.y + other.y
        };
    }
}

let p1 = Point {
    x: 5,
    y: 4
};

let p2 = Point {
    x: 5.0,
    y: 4.0
};

let resultant = p1 + p2;